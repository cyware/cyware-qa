cyware-agent1:
  - regex: ".*Sending integrity control message: (.+)$"
    path: "/var/ossec/logs/ossec.log"
    timeout: 60

cyware-manager:
  - regex: ".*Agent 001 query: syscheck delete"
    path: "/var/ossec/logs/ossec.log"
    timeout: 60
